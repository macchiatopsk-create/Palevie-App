import HistoryClient from "@/components/HistoryClient";
export default function DashboardPage(){return <div className="app-wrap"><div className="app-title"><div><div className="eyebrow">My Palevie</div><h1>Your decisions.</h1><p>Shopping checks are always saved locally; signed-in users also get cloud history when Supabase is configured.</p></div></div><HistoryClient/></div>}
