# Palevie MVP v0.4 — 설치 및 다음 단계

## 1. 지금 바로 돈 안 쓰고 테스트 가능한 기능

- `/quiz` : 12문항 퍼스널컬러 퀴즈
- `/analyze` : 상품 사진 업로드 → 자동 색 추출 → 색 확인/수정 → BUY/MAYBE/SKIP
- `/skin` : 스킨케어 취향 프로필
- `/shop` : 데모 상품을 이용한 메이크업/스킨케어 추천
- `/dashboard` : 로컬 분석 기록
- `/admin/growth` : 이 브라우저 기준 퍼널/광고비/수익가정 계산기

AI 없이도 위 핵심 루프가 작동하도록 설계되어 있습니다.

## 2. Supabase 연결

1. Supabase 프로젝트 생성
2. SQL Editor에서 `supabase/schema.sql` 실행
3. `.env.example`을 `.env.local`로 복사
4. 아래 값 입력

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

연결 후:
- 이메일 magic-link 로그인
- 컬러/스킨 프로필 클라우드 동기화
- 분석 기록 클라우드 저장
- Free/Plus 상품체크 횟수 서버 제한
- 이벤트/affiliate click/AI usage 기록

이 활성화됩니다.

## 3. OpenAI AI 스캔 연결

```env
OPENAI_API_KEY=
OPENAI_VISION_MODEL=
PALEVIE_AI_ENABLED=true
PALEVIE_AI_USER_DAILY_CALL_CAP=2
PALEVIE_AI_MONTHLY_CALL_CAP=10000
PALEVIE_AI_MONTHLY_BUDGET_USD=100
PALEVIE_AI_ESTIMATED_COST_PER_SCAN_USD=0.01
```

`/diagnose`만 AI가 필수입니다. 일반 상품 색 판정에는 AI가 필요 없습니다.

상품 설명문 AI도 쓰고 싶으면:

```env
OPENAI_EXPLAIN_MODEL=
PALEVIE_AI_EXPLANATIONS_ENABLED=true
```

초기에는 `false` 유지 권장. 기본 설명문이 이미 deterministic으로 나옵니다.

## 4. Stripe 연결

```env
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_MONTHLY_PRICE_ID=
STRIPE_YEARLY_PRICE_ID=
```

현재 코드 흐름:

`/pricing` → signed-in checkout → Stripe → webhook → Supabase `profiles.plan = plus` → 월 100 product checks

환경변수가 없으면 실제 결제를 만들지 않고 setup/demo 메시지만 보여줍니다.

## 5. Affiliate / 쇼핑몰 연결

현재 구조는 특정 두 쇼핑몰에 묶이지 않습니다.

지원 retailer ID:
- Amazon
- Sephora
- Olive Young
- YesStyle
- Target
- Walmart
- iHerb

실제 출시 전에는 각 프로그램 승인 후 받은 **정식 상품 URL/feed/affiliate URL**을 `ProductOffer` 형식으로 넣으세요. 현재 `data/products.ts`는 데모입니다.

클릭은 `/go/[offerId]`를 통과하며 UTM/creator/ref와 함께 기록됩니다.

구매 postback은 네트워크마다 서명 형식이 다르므로, 각 네트워크용 verifier/adapter를 만든 후 내부 표준 endpoint `/api/affiliate/postback`으로 변환해서 넣는 구조입니다.

## 6. 광고 $1,000 테스트

`/admin/growth`에 실제 사용한 금액을 입력합니다.

추적해야 할 핵심:
- page view → quiz start
- quiz start → complete
- complete → product check / shop
- result share
- affiliate outbound click
- 실제 purchase/commission (네트워크 postback 연결 후)

같은 페이지에 월 사용자, 클릭률, 구매율, AOV, affiliate %, Plus 전환율, 운영비를 직접 바꿔보는 unit economics 계산기가 있습니다. 기본값은 가정일 뿐 실제 성과값으로 교체해야 합니다.

## 7. Claude에게 넘길 때

프로젝트 ZIP 전체와 `PALEVIE_CLAUDE_HANDOFF_v0_4.md`를 같이 보내세요.

중요: Claude에게 "새로 만들지 말고 이 코드베이스를 source of truth로 계속 수정"하라고 되어 있습니다.

## 8. 실제 런칭 전에 외부에서 반드시 필요한 것

- affiliate 프로그램 승인 및 실제 feed
- 실제 상품 이미지 사용권
- 셀카 기반 퍼스널컬러 정확도 평가/전문가 라벨링
- 개인정보/Terms/retention/deletion 법률 검토
- production monitoring / abuse rate limiting
- admin 인증
- 결제/세무/법인/대출 등 사업 운영 설정
